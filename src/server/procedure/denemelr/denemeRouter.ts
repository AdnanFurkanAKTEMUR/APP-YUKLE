import { privateProcedure, publicProcedure, router } from "../../trpc";
//import denemeMaili from "./mail";

export const DenemeRouter = router({
  // sendMailDeneme: publicProcedure.mutation(async ({ ctx, input }) => {
  //   const result = await denemeMaili();
  //   return result;
  // }),
});
